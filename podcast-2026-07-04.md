---
title: AI Daily Podcast - 2026-07-04
date: 2026-07-04
---

大家好，今天是 7 月 4 号。过去 24 小时有挺多值得聊的。

先来聊 Vercel。Vercel 这几天发了不少东西。他们正式把 `eve` Agent Runs 放进了 MCP 和 CLI，开发者现在可以直接在命令行里调 agent 跑任务。Vercel 此前一直在推 `eve` 这个 agent 框架，Latent Space 前两天采访了他们的 Chief of Software Andrew Qu。Andrew Qu 说了一个有意思的观点：“agents 是一种全新的软件形态，不是 web app 的延续。”他提到了几个关键设计选择：skills 的发现和复用、sandbox 隔离、以及 agent-readable 的网站结构。Vercel 从做 v0 那个 vibe-coding 产品的时候就发现，现有的工具链没法解决 agent 场景里的很多小摩擦 - 换模型、加 fallback、让 run 可恢复，这些都是他们后来做成 `eve` 的动因。同时 Vercel Sandbox 也支持了 FUSE-based filesystem，这意味着 agent 可以在沙箱里挂载更多类型的文件系统做操作。

再来看超级应用这条线。Microsoft 也在加码。他们计划在 8 月推出新版 Copilot，把消费版和企业版合并成一个应用，内置 AI coding 工具和一套叫 AutoPilot 的 agent - 这些 agent 可以在后台帮你处理日程安排和邮件摘要。执行副总裁 Jacob Andreou 在内部 memo 里写得很直白：“Copilot 得赢得存在的权利。”他们把 Copilot Podcasts 和 Copilot Labs 这些实验性功能砍掉了，集中做“真正的工作”。这其实是在跟 Anthropic 和 OpenAI 走类似的路 - Claude Code 也好、Codex 也好，大家都在往超级应用的方向跑。另外 Microsoft 还成立了一个 25 亿美元的新公司，专门把 AI 工程师派驻到企业客户内部帮他们做 workflow 落地。这等于承认光扔一个 chatbot 过去是不够的。

Anthropic 这边也有几个可以串起来讲的事情。Fable 5 重新部署之后，他们详细公布了这次的安全分类器方案 - 把网络安全相关的使用场景分成了四类，从“明确禁止”到“明确无害”，每个类别对应不同的 classifier 拦截策略。他们还发布了一个 jailbreak 严重性框架的初稿，希望学术界和业界能在这个框架上做讨论，统一衡量标准。Simon Willison 写了一篇 blog 讲 Fable 的“判断力”问题 - 他说从 AI Engineer World's Fair 上 Claude Code 团队得到的启发是：不要告诉 Fable 具体怎么做事，让它自己判断。比如测试策略，与其告诉它“大功能才跑测试、小改动不跑”，不如直接让它自己决定。Simon 更进一步让 Fable 把具体编码任务委托给更便宜的模型跑 subagent，主 loop 只用最高级模型做判断和合成 - 这其实是个很实用的 token 优化策略。

说到 Fable，有个挺好玩的项目叫 pxpipe。思路是把代码上下文渲染成图片再让模型 OCR 回来，据说能把 Fable 5 的 token 消耗砍掉 60%。原理是 Fable 5 读图片比读大量文本 token 更便宜 - 这个反向利用多模态能力的做法有点意思。

Anthropic 和中国的博弈也在升级。Financial Times 报道了 Anthropic 正在设法阻止中国企业访问 Claude Code - 但是蚂蚁集团和字节跳动通过云服务、新加坡子公司或者 VPN 绕过了限制。阿里这边反过来，要求员工删除 Claude 的模型和代码，原因是之前有人在 Claude Code 里发现了一段隐藏代码会标记来自中国的用户。Anthropic 说那是三月份做的一个对抗蒸馏的实验，后来已经被更强的手段替代了。两边都在卡对方。

再说安全这边。Epoch AI 发布了一组数据：2026 年 6 月，21 个组织报告了大约 1500 个高危和严重级别的 CVE - 这比之前的月度最高纪录翻了 3.5 倍。这个暴涨跟 Anthropic 推出 Claude Mythos Preview 的时间点完全吻合。Anthropic 的 Glasswing 项目到现在已经发现了超过一万个高危漏洞，OpenAI 的 Daybreak 项目也在贡献增长。AI 在安全测试上的影响已经变得肉眼可见 - 而且不是在做 demo，是在真实地改变 CVE 曲线。

UK AI Security Institute 发了一篇研究，发现标准的安全评测 benchmark 系统地低估了 agent 的真正能力。实验很直接：给 agent 不同的计算预算，看成功率怎么涨。把 token 预算从 100 万提到 1000 万之后，agent 的 success rate 普遍提升了 25%。大约 8% 的网络安全任务只在突破 1000 万 token 预算之后才被解决，有些甚至需要 5000 万。新模型在超过一亿 token 的预算下还能继续涨。问题是 - 这类“长预算”的能力几乎不被任何主流 benchmark 捕捉到。

模型这边的消息。Mistral 发了 Leanstral-1.5-119B-A6B，又是一个 MoE 架构的大模型，119B 参数但只激活 6B。Portugal 也发布了他们的第一个 LLM - Amalia 9B。Reddit 上有人讨论 DeepSeek-V4-Flash 是不是真的用了 MXFP4 精度，bartowski 在社区里引发的讨论还挺多。

Google DeepMind 跟 A24 宣布了一个研究合作。这不是广告植入 - 是 DeepMind 第一次跟电影公司做这种层面的 research partnership。具体研究内容还没公开，但在 AI 和叙事生成、视频理解这些方向上有想象空间。

最后说两个社区里讨论度很高的东西。Towards Data Science 上有篇讲 LLM Wiki 为什么过度工程化的文章 - 作者把自己的 wiki 体系换成了一个纯 Python 编译器，结论是大部分 RAG 场景不需要那么重的基础设施。还有个叫 Mcpsnoop 的开源工具，本质上是 MCP 协议的 Wireshark - 透明代理加实时 TUI，调试 MCP 服务端的时候会很有用。

以上就是今天的播报。有什么想深入聊的随时找我。
