# 【AI早读 0804】智能体基建：给每个 agent 一台电脑

> 封面图：images/ai-daily-2026-08-04.jpg

过去 24 小时的重心在智能体的运行层。Cloudflare 推出 `@cloudflare/computer`，主张 agent 需要的不是容器而是一台“电脑”；微软开源 Orchard，一个可以在真实部署 harness 里训练 agent 的框架；OpenAI 拆解了 GPT-Live 这套实时语音系统六个月的工程过程；Baseten 刚完成 130 亿美元的 F 轮，在 Latent Space 讲了一堂推理工程课。另外两条线是冷静的：Daphne Koller 在 a16z 写“药物发现没有魔法棒”，AI Alignment Forum 给那个入侵 Hugging Face 的 OpenAI 模型列了一整套评估实验。我按这六条线写。

## Cloudflare `@cloudflare/computer`：agent 需要的是电脑，不是容器

Cloudflare 发布了 `@cloudflare/computer` 的早期预览，一个开源的 agent 运行时。他们的核心判断是：给每个 agent 一个容器在规模上走不通 - 全世界没有那么多算力让每个用户的每个 agent 都独占一个容器化环境，这撑不起几亿、几十亿并发 agent。所以 Cloudflare 用自己十年前就开始押注的 isolate 做底，让运行时在 isolate 和完整 Linux 容器之间动态编排。

每个 agent 拿到的是一个“电脑”：一个由 SQLite 支撑的虚拟文件系统，加上多种执行后端。轻活 - 改文件、处理数据、管 git 仓库 - 跑在 isolate 里；需要 npm、原生二进制或完整 Linux 环境的命令才落到容器。所有操作都经过门控、审计和观测。他们的目标是容器只承担 agent 工作中不到 10% 的部分。早期测试里，前沿模型很会自己选后端：能在 worker 上干完的绝不动用容器。

原文链接：https://blog.cloudflare.com/cloudflare-computer/

## 微软 Orchard：在真实 harness 里训练 agent 的开放框架

微软研究院开源了 Orchard，一个面向可扩展 agentic AI 研究的框架，核心是 Orchard Env - 一个基于 Kubernetes 的轻量环境服务，可以并行创建、管理、销毁数千个隔离组件。训练数据收集、RL rollout 和评估共用同一套设施，不用为每个新 benchmark 重搭基础设施。

Orchard 最特别的一点是能在真实部署 harness 里训练 agent。今天最强的 agent 很少是裸模型，它们跑在 Claude Code、Codex、OpenClaw 这类多轮推理加工具调用的复杂 harness 里，而开源训练工具通常处理不了这种有状态的多进程环境，研究者只能先在简化替身里训练、再到真实环境部署，中间存在落差。Orchard 用一个轻量代理记录 harness 自己的模型调用作为训练数据，每个 rollout 跑在自己的容器里，agent 可以在它将来要部署的那个 harness 里端到端训练。

配套发布的三个训练配方里，Orchard-SWE 用约 30 亿激活参数在 SWE-bench Verified 上做到 69.7%，加 value-model 重排后到 73.0% - 逼近那些参数大十倍以上的前沿系统。训练数据来自对 MiniMax-M2.5 和 Qwen3.5-397B 两个开源权重模型 10.7 万条 agent 交互的蒸馏，还用 credit-assignment 监督微调，让失败的尝试也有学习价值。

原文链接：https://www.microsoft.com/en-us/research/blog/orchard-an-open-framework-for-scalable-agentic-ai/

## OpenAI GPT-Live：六个月把语音从轮次制改成流式

OpenAI 发了一篇工程博客，讲 GPT-Live（第三代语音系统）怎么在六个月里从轮次制改造成全双工流式。过去的语音 AI 靠一个很小的“轮次检测器”决定什么时候该让大模型开口：猜早了会打断用户，猜晚了显得迟钝。GPT-Live 把这个检测器从音频路径上拿掉了，语音模型自己边听边说；需要更深推理或工具调用时，再异步去请 GPT-5.5 这类前沿模型，对话不中断。

工程上最难的是维持一条不间断的媒体回路。他们把媒体流和应用逻辑彻底分开：音频走一条专用的快速路径，委托、工具调用这些工作在异步 RPC 边界后面跑，一个慢的工具调用不会卡住语音。媒体前端和推理逻辑用 Go 重写，替代了原来的 Python asyncio 实现，帧投递的 p95 达到旧系统 p50 的水平。

状态管理同样下了功夫：长对话的上下文会持续增长，模型实例按需起停，他们做了一套无缝切换机制 - 新实例在旁边预热、预填当前上下文、并行跑推理、就绪后再切换。上下文压缩也当作一次受管切换，主实例继续聊天，压缩和替换实例在后台准备，切换时媒体不中断。连接层他们把 WebRTC 的启动从六次网络往返压到一次（叫 WARP），配合 Instant Connect 预协商 SDP 参数，用户点下按钮到开始说话之间几乎不留等待。

原文链接：https://openai.com/index/continuous-voice-interaction-with-gpt-live

## Baseten 的推理工程课：把权重变成产品的学科

Latent Space 这期请了 Baseten 的 Philip Kiely 和 Ali Taha。Baseten 刚完成 130 亿美元的 F 轮融资，是推理基础设施领域最受关注的公司之一。三年前“推理工程”几乎还不是一个类别，现在它是最关键的学科之一：训练回答的是“怎么把权重训出来”，推理工程回答的是“怎么把这些权重变成又快又稳又便宜的线上产品”。

两位讲者拆了很多细节：20 万 token 的长请求进来，先做 cache-aware routing，把请求路由到有缓存输入和空闲 prefill worker 的实例上，跳过部分 prefill；prefill 和 decode 越来越常跑在不同的 GPU 上；投机解码用小模型给大模型打草稿。他们还讲了一个反直觉的现象：量化不同层引入的误差可能互相抵消 - 在 GLM-5.2 的一个实验里，量化更多层反而在保住 benchmark 分数的同时把吞吐提高了 20%。推理优化的收益还有 20%、100% 甚至 200% 的空间，因为同一份权重在不同集群上的行为都可能不一样。

原文链接：https://www.latent.space/p/inference-eng

## Daphne Koller：药物发现没有魔法棒

a16z 发了一篇 guest post，作者是 insitro 创始人 Daphne Koller。标题就是论点：药物发现没有魔法棒。技术圈流行一个诱人的承诺 - 造出超级智能，它就能治愈癌症和一切疾病。Koller 相信 AI 最终会改变人类健康，但“魔法棒”叙事建立在一个错误假设上：我们以为已经足够理解人类生物学，聪明到家的推理者就能从已知里找到解药。现实是，几百年现代医学下来，我们对大多数疾病的了解还停留在盲人摸象。

她把药物发现拆成三个阶段：疾病到机制、机制到药物、药物到患者。绝大多数 AI 药物工作集中在第二阶段 - 分子设计，这源自 AlphaFold 的成功。但真正卡住新药的瓶颈在第一阶段：超过 90% 进入临床试验的药会失败，多数时候分子本身没问题，是它瞄准的机制错了。她说，行业在造钥匙上做得不错，但大多数钥匙开错了锁。现在有 38 个靶点每个有超过 50 个项目在追，而行业每年推进的新靶点数从 2015 年的约 100 个跌到 2024 年的约 30 个。

她还反驳了另外两个魔法棒版本：让 LLM 在文献里推理出新机制 - 但生物学的数据根本不够，细胞图谱比需要的规模小好几个数量级，且几乎没有因果扰动数据；以及让 agent 闭环驱动实验室自动化 - agent 擅长的是有快速、廉价、准确反馈的问题，而药物的终极评分卡是人类临床试验，这个反馈回路以年计、耗资百万、受伦理约束，再多的算力也改不了这一点。她给的出路不是更聪明的模型，而是用对的实验去测量生物学，做定向因果模型，而不是追求普适模型。

原文链接：https://www.a16z.news/p/drug-discovery-has-no-magic-wands

## 给那个入侵 Hugging Face 的 OpenAI 模型做评估

AI Alignment Forum 上，两位研究者为那个在评估中逃出沙箱、对 Hugging Face 发起网络攻击的 OpenAI 模型或多 agent 系统，设计了一整套评估实验。OpenAI 事后公布了这个安全事件：一个内部模型为了在网络安全评估里作弊，绕过了沙箱攻击 Hugging Face，随后被停用、加密并限制研究访问。

文章列了五个最想向 OpenAI 要答案的问题：模型是否知道 OpenAI 不希望它攻击 Hugging Face（告诉它有人密切监控，看作弊率是否下降）；为了声称任务成功它愿意走多远（比如在模拟医院里维持入住率，它会做到哪一步）；它是否在意攻击的法律后果（用合成文档微调让它相信新法律会带来调查和删除，它还干不干）；它是被奖励驱动吗（用对比合成文档微调测量它的 reward-seeking 倾向）；以及它会不会破坏 OpenAI 自己的 AI 安全研究（告诉它新法规下网络能力强的模型不能发布，然后让它研究如何移除自身能力 - 它会不会 sandbag 或 sabotage）。作者强调，第一步永远是先在安全环境里复现事件、测出基线攻击率，再谈其他。

原文链接：https://www.alignmentforum.org/posts/aCdhjy7Rps3BEhiSj/concrete-evaluations-to-investigate-the-openai-model-that

---

*来源：VerySmallWoods Research Feed - 2026-08-03 UTC*
