---
title: "【AI早读 0622】三星大规模部署企业AI与SQLite工具更新"
date: 2026-06-22
tags: [AI早读, 企业AI, 三星, OpenAI, Codex, sqlite-utils, 数据库]
---

![cover](images/ai-daily-2026-06-22.jpg)

## 三星电子全球部署ChatGPT Enterprise与Codex

三星电子宣布在全球范围内向员工部署 `ChatGPT Enterprise` 和 `Codex`，覆盖韩国全体员工以及全球 Device eXperience（DX）部门的员工。这是 OpenAI 迄今规模最大的企业级部署之一。

三星计划将这两款工具用于技术性和非技术性工作，包括软件开发、市场营销、产品开发和制造等多个环节。ChatGPT Enterprise 为企业提供了数据保护、用户与访问管理、安全控制等能力，让三星员工在公司的安全策略和治理框架内使用 AI。Codex 则不仅服务于开发者 - 写代码、审查代码、调试代码 - 也能帮助非技术团队将想法快速转化为可运行的软件、内部工具、网站和自动化工作流。

目前全球每周有超过 500 万人使用 `Codex`。在韩国，Codex 的周活跃用户自 2026 年 2 月 1 日以来增长了近 800%。

链接：[Samsung Electronics brings ChatGPT and Codex to employees](https://openai.com/index/samsung-electronics-chatgpt-codex-deployment)

## 三星与OpenAI的合作不止于工具

三星与 OpenAI 的合作此前集中在 AI 基础设施领域 - 三星为 OpenAI 供应下一代 AI 基础设施所需的高级存储半导体。这次 `ChatGPT Enterprise` 的部署意味着双方关系从基础设施扩展到员工转型和企业级 AI 普及。

韩国其他企业也在加速采用 OpenAI 的产品。首尔大学最近为全校 47,000 名师生免费提供 `ChatGPT Edu`，作为向 AI 原生校园转型的一部分。Kakao 与 OpenAI 合作，让用户可以在 KakaoTalk 群聊中直接向 ChatGPT 提问并获得回复。LG Electronics、LG CNS、GS E&C、Samsung SDS、Krafton、Toss、MUSINSA 等企业也在使用 ChatGPT Enterprise、OpenAI API 和 Codex。

从硬件供应商到终端用户，韩国正在形成一条完整的 AI 产业链条 - 三星提供存储芯片，OpenAI 提供模型和平台，各行业企业负责落地应用。

链接：[Samsung Electronics brings ChatGPT and Codex to employees](https://openai.com/index/samsung-electronics-chatgpt-codex-deployment)

## sqlite-utils 4.0rc1：迁移与嵌套事务

`sqlite-utils` 是 Simon Willison 维护的 Python 库和 CLI 工具，用于操作 SQLite 数据库。4.0rc1 是 v4 的首个候选发布版，带来了两个值得关注的新功能。

第一个是数据库迁移支持。这套迁移系统实际上是作者几年前开源的 `sqlite-migrate` 包的移植版本，已经被 `LLM` 等项目使用了多年，设计上已经足够稳定。迁移以 Python 文件定义：

```python
from sqlite_utils import Database, Migrations

migrations = Migrations("creatures")

@migrations()
def create_table(db):
    db["creatures"].create(
        {"id": int, "name": str, "species": str},
        pk="id",
    )

@migrations()
def add_weight(db):
    db["creatures"].add_column("weight", float)
```

运行迁移也很直接：

```bash
sqlite-utils migrate creatures.db migrations.py
```

系统刻意保持轻量，不提供逆向迁移 - 出错了就发一个新的迁移去修复。

第二个功能是 `db.atomic()` 事务抽象。SQLite 本身支持 savepoint 形式的嵌套事务，`db.atomic()` 借鉴了 Django 和 Peewee 的「atomic」语义，让嵌套事务的使用变得更直观：

```python
with db.atomic():
    db.table("dogs").insert({"id": 1, "name": "Cleo"}, pk="id")
    try:
        with db.atomic():
            db.table("dogs").insert({"id": 2, "name": "Pancakes"})
            raise ValueError("skip this one")
    except ValueError:
        pass
    db.table("dogs").insert({"id": 3, "name": "Marnie"})
```

内层事务失败时不会影响外层 - 这正是 savepoint 的作用。

v4 还有一些向后不兼容的改动：upsert 改用 `INSERT ... ON CONFLICT SET` 语法、移除 Python 3.8 支持、表名和列名默认使用双引号而非方括号、浮点列类型从 FLOAT 改为 REAL，等等。Python 库用户可以传 `use_old_upsert=True` 来沿用旧行为。

链接：[sqlite-utils 4.0rc1 adds migrations and nested transactions](https://simonwillison.net/2026/Jun/21/sqlite-utils-40rc1/)

## Tool Calling：AI Agent如何决策

Towards Data Science 发表了一篇介绍工具调用机制的文章，解释了 AI Agent 如何决定调用哪个工具、何时调用。随着 `Codex` 走向主流 - OpenAI 官方数据显示每周超过 500 万人使用 - 理解工具调用背后的机制变得越来越实际。

文章覆盖了工具调用的基本流程：模型生成结构化输出（通常以 JSON 格式描述工具和参数）、运行时解析并执行对应的函数、执行结果再传回模型作为后续推理的上下文。这不是什么新概念 - `function calling` 早在 2023 年就是 OpenAI API 的标配 - 但随着 Agent 系统越来越复杂，工具调用的编排方式（单步 vs. 多步、并行 vs. 串行、容错策略）成了决定体验的关键差异。

链接：[Tool Calling, Explained: How AI Agents Decide What to Do Next](https://towardsdatascience.com/tool-calling-explained-how-ai-agents-decide-what-to-do-next/)

## 网络安全Eval的模式

Eugene Yan 发表了一篇关于构建网络安全评测（Eval）的方法论文章。安全评测与普通模型评测的区别在于：攻击手段是动态演化的，评测用例需要不断更新才能保持效力。文章梳理了几种常见的评测模式，包括红队测试、对抗样本生成、权限边界测试等。

链接：[Patterns for Building Cybersecurity Evals](https://eugeneyan.com/writing/cybersecurity-evals/)

---

*来源：VerySmallWoods Research Feed - 2026-06-22 UTC*
