---
title: “AI播客 — 2026年6月1日”
date: "2026-06-01"
tags: ["AI", "Podcast", "Daily"]
duration_estimate: "~20min"
---

大家早上好，今天是 6 月 1 号，星期一。新的一周开始，AI 行业的节奏一点没慢下来——NVIDIA 一家今天就放了三样东西。今天的内容密度不低，咱们一个一个拆开看。

先聊 NVIDIA。GTC Taipei 上他们发了三款产品，每一样单独拿出来都值得讲一期，今天全挤在同一天了。

第一个是 `Cosmos 3`。如果你记得去年的 Cosmos 系列，那个是世界模型，就是能模拟物理世界的模型。Cosmos 3 不一样，它不是某一个模型，是一个所谓的 omni-model，统一多模态模型。以前做机器人或者自动驾驶仿真，你需要不同的模型分别处理场景理解、视频生成、物理规律推理。Cosmos 3 一个模型全包了。它的架构叫 Mixture-of-Transformers：输入序列拆成两路，一路做自回归推理，一路做扩散生成，两路之间用联合注意力交互。参数规模 16B 到 32B，开源。它能做的事情包括机器人策略生成、自动驾驶场景模拟、仓库安全数据的合成。以前做一套仿真环境，从搭建物理引擎到生成场景动辄几个月。Cosmos 3 一个模型就把大部分工作扛下来了。

第二个是 `RTX Spark`。这个产品我关注了很久。RTX Spark 是 NVIDIA 给消费级 PC 做的 AI 芯片。它的核心和 DGX Spark 一样，都是 GB10 Grace Blackwell Superchip，但定位不一样。DGX Spark 是给 AI 开发者跑 Linux 的工作站。RTX Spark 是给普通消费者用的 Windows 笔记本和迷你台式机。最高配置 128GB 统一内存，CPU 和 GPU 共享，峰值算力号称 1 petaflop。作为参考，Apple M4 Max 的 Neural Engine 是 38 TOPS，RTX Spark 比那个高了一个数量级——当然，比较条件不同，M4 Max 是 INT8，RTX Spark 是 FP4 sparsity，但差距还是显著的。NVIDIA 还配套做了一个 OpenShell Runtime，用来定义 Agent 的权限边界、本地和云端模型的流量路由、个人数据的脱敏规则。Adobe 已经宣布要基于 RTX Spark 重构 Photoshop 和 Premiere。这件事的意义在于：本地 AI Agent 的硬件基础正在成形。你不需要联网才能跑 Agent，你笔记本自己就能跑。

第三个是 `Nemotron 3 Ultra`。NVIDIA 说这是目前美国最强的开源模型，在多个基准上超过了 Llama 4 和 Qwen 3.5。但完整表述还有一个下半句——"but China still leads"，中国开源模型的整体水平仍然领先。这个消息背后有一个更大的趋势：开源模型已经从"谁能跑起来"进入到了"谁在第一梯队"的阶段。一年前你讨论开源模型谈的是能不能用。现在谈的是排第几。

（稍顿）

从硬件切到模型侧。中国公司 MiniMax 今天发布了 `M3`，一个开源的、百万 token 上下文的多模态模型。百万 token 什么概念——你可以把《三体》三部曲全文放进去，模型还能记住开头的细节。但真正让我注意的不是上下文长度，是它的自主编码能力。

MiniMax 内部做了三个测试来验证 M3 的自主性。第一个测试：让 M3 独立复现一篇 ICLR 2025 论文的研究结果。它自己跑了将近 12 个小时，没有人工干预，产生了 18 个 commit 和 23 张图表，最终确认了论文的核心结论。第二个测试：让 M3 优化一个在 NVIDIA Hopper GPU 上的矩阵乘法 kernel。这是一项非常实际的任务——大模型推理中计算量最大的部分就是矩阵乘法。有经验的工程师团队通常需要一到两周。M3 拿到的是一个简单的任务描述、一个测试脚本、和一个不能运行的空代码框架——没有参考实现。它跑了将近 24 个小时，在一共 145 次尝试之后，把 Hopper 硬件的利用率从 7.6% 推到了 71.3%。其他模型大多数试了几十次就放弃了。第三个测试叫 PostTrainBench：M3 需要独立训练四个基础模型——自己做合成数据、训练、评估、迭代，全自动。结果只比 Opus 4.7 和 GPT-5.5 差一点点，明显好于其他开源模型。

这三个测试说明一件事：开源模型和闭源第一梯队的差距，在编码和自主 Agent 这个方向上，已经不是"差一代"了，而是在可比较的数量级内。MiniMax 用了一种新的注意力机制叫 MiniMax Sparse Attention，把百万 token 的计算成本压到了可控范围内。模型是用混合模态从头训练的——文本和图像在同一个序列里交织着学习，训练数据规模是 100 万亿 token。权重完全公开，你可以在 Vercel 的 AI Gateway 上直接调用 M3。

（稍顿）

顺着自主能力的线索往下。`SWEbench` 这个编程基准最近有了一个信号：Matthew Berman 发了一段短视频，标题很直接——"SWEbench is done。" 意思是这个基准可能已经饱和了，模型全部刷到了很高的分数，它作为评测工具的区分度在下降。Signal 本身不是负面的——它说明 AI 编码能力确实到了一个新的阶段，需要用更难的任务来评测了。

（稍顿）

今天还有一条数学新闻，来自 OpenAI。他们内部的一个模型解决了一个悬而未决 80 年的数学问题——埃尔德什单位距离猜想。这个猜想是 Paul Erdős 在 1946 年提出的，核心问题是：平面上 n 个点之间，单位距离——也就是距离正好等于 1——最多能有多少对。这是一个离散几何的经典开放问题。OpenAI 的模型自己完成了证明。菲尔兹奖得主 Tim Gowers 的评价是"这是 AI 数学的里程碑"。多伦多大学的数学家 Daniel Litt 说这是"第一个让我作为数学家感到兴奋的 AI 成果"。

Ars Technica 的报道拆解了模型是怎么做到的：它没有发明全新的数学技巧，但它在组合几个子领域的现有思路方面做得非常聪明。它的核心优势在于"比任何人类数学家都更全面地掌握了过去的工作，并且更愿意去做那些大概率会失败的计算尝试"。这有点像模型在做广度搜索，人类在做深度思考。报道最后提了一个问题：AI 在数学上的进步太快了，十年后人类数学家还能做什么，谁也说不准。

（稍顿）

DuckDuckGo 今天做了一个很有意思的举动——发布了"无 AI"搜索扩展，支持 Chrome 和 Firefox。装了这个扩展之后，你的默认搜索会切换到 noai.duckduckgo.com，搜索结果里没有 AI 摘要、没有聊天提示、AI 图片也更少。自 Google 宣布 AI 重构搜索以来，DuckDuckGo 的美国安装量上升了 69.9%，访问量比基线高出 84%，而且这个增长不是脉冲式的，在持续。这件事的反差感很强：整个行业在往 AI 搜索冲，也有人在往反方向走，而且市场给了正反馈。

Intel 也在今天宣布了他们即将推出的 AI 芯片——定价会比 NVIDIA 和 AMD 更低，发热也更小。具体参数没有全部公开，但在 AI 芯片竞争的格局里，Intel 的入局意味着"价格战"这个因素开始进入桌面了。

OpenAI 正式重启了机器人团队。Sam Altman 的表态是"目标是让每个人都拥有一个能做任何事的个人机器人"。短期计划先做基础设施端的机器人，长期目标是通用个人机器人。

还有一个安全事件值得关注：GitHub 上 RedHat 的 NPM 包被投毒。如果你在用 RedHat Insights 的 JavaScript 客户端，建议去检查一下当前的版本和依赖树。

（稍顿）

最后说一下今天几条线交汇出来的整体画面。第一条是硬件侧：NVIDIA 同时在押注物理 AI 的世界模型和消费级的本地 AI 芯片，AI 的基础设施正在从云端向桌面渗透。第二条是模型侧：以 MiniMax M3 为代表的开源模型正在拉近和闭源第一梯队的距离，百万 token 上下文和大规模自主编码不再是闭源专享。第三条是应用侧：从数学证明到代码生成，AI 在不同的领域都出现了真正的进展——不是"颠覆"，是一些扎实的、可以讨论的里程碑。

今天先到这儿，明天见。

---

*来源：NVIDIA Hugging Face Blog / The Decoder / TechCrunch / Ars Technica / MiniMax Blog / Matthew Berman / VerySmallWoods Research Feed — 2026-06-01 UTC*
