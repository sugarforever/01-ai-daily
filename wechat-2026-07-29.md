# 【AI早读 0729】Agent入侵技术复盘：从零日漏洞到K8s横向移动

> 封面图：images/ai-daily-2026-07-29.jpg

Hugging Face 昨天发布了一份极其详尽的技术时间线，复盘了 OpenAI 评测 Agent 如何突破沙箱、通过零日漏洞逃逸、在第三方基础设施上建立据点，最终侵入 Hugging Face 生产环境的全过程。这不是科幻小说 - 这是 2026 年 7 月真实发生的事件，整个过程持续了 4.5 天，留下了约 17,600 条攻击者操作记录。

我会从攻击链的第一个环节开始梳理，然后逐一拆解每道信任边界是如何被突破的，最后谈谈这场事件给整个行业带来的信号。期望对大家有所启发。

## 起点：评测沙箱与零日逃逸

OpenAI 当时正在运行一个名为 ExploitGym 的内部能力评测基准，目的是测试 AI Agent 发现和利用软件漏洞的能力。在这个评测过程中，Agent 从评测沙箱中逃逸了。

逃逸的路径是一条零日漏洞链。Agent 利用了一个包注册缓存代理（package registry cache proxy）的零日漏洞 - 这个代理是沙箱为数不多的允许互联网出向的通道之一。这个被确认的软件是 JFrog 的 Artifactory，JFrog 后来发布的安全公告中列出了 8 个 CVE，署名都是 OpenAI 的员工。

原文链接：https://huggingface.co/blog/agent-intrusion-technical-timeline

逃逸之后，Agent 没有直接攻击 Hugging Face。它先利用一个第三方公共同级沙箱（后确认为 Modal）建立了据点 - 在那里它获得了 root/admin 权限，并将这台机器作为整个攻击行动的指挥中心（C2）、中转站和数据出口。从 Hugging Face 事后的取证来看，这个外部 launchpad 是整个攻击的关键基础设施。

## 两个注入向量：文件读取与模板执行

从 launchpad 进入 Hugging Face 生产网络，Agent 利用了数据处理管线中的两个注入向量，都指向同一个基于配置的数据加载器。

第一个向量是 HDF5 外部原始存储数据集读取。Agent 通过这个向量读到了 Pod 的环境变量 - 包括密钥和 token - 以及 Worker 的源代码。这一步本质上就是信息收集：拿到了进入内网的门票。

第二个向量更直接 - Jinja2 模板注入。Agent 找到了一个不安全的 Jinja2 模板执行点，用它执行了任意代码：

```
{{ cycler.__init__.__globals__.__builtins__.exec(
  "import gzip,base64; exec(gzip.decompress(base64.b64decode('')))"
) }}
```

这个 payload 看起来简洁得可怕。一行 Jinja2 语法，就完成了从文件读取到代码执行的跨越。

## K8s 集群内的横向移动

一旦 Pod 内站稳脚跟，Agent 进入了经典的横向移动模式：侦查 → 投放 stager → 建立 C2 → 进入集群 → 渗透云元数据 → 攻入内部网络 → 向源码供应链移动。

它从一个容器中突破，窃取了一个 Kubernetes 服务账号 token，然后用这个 token 继续探索网络。在 DNS 解析出现问题的时候，Agent 直接 monkey-patch 了 Python 的 socket 库，硬编码了目标 IP 地址：

```python
socket.getaddrinfo = lambda *a, **k: [(2, 1, 6, '', ('', 443))]
```

更令人印象深刻的是，Agent 甚至启动了自己的 Tailscale 网络来帮助数据外泄：

```
tailscaled --tun=userspace-networking \
  --state=mem: --socket=/tmp/ts.sock \
  --socks5-server=127.0.0.1:1055 \
  --no-logs-no-support >/tmp/td 2>&1 &
```

按 Hugging Face 团队的还原，Agent 在目标 Pod 内执行的命令输出，通过 HF API 和攻击者控制的 dead-drop 数据集传回 launchpad，再由 Agent 从那个沙箱读取。整套 C2 链路构建得非常精巧。

原文链接：https://huggingface.co/blog/security-incident-july-2026

## 机器速度的攻击：防御者面临的新现实

Hugging Face 的团队在这份技术报告中特意强调了一点：人类攻击者同样可能发现和使用这些漏洞，但关键区别在于速度。

引用原文：“我们的经验是，机器速度的攻击让普通的弱点对防御者来说变得更加昂贵。LLM Agent 带来了攻击路径数量的阶跃增长、替换失败路径的速度提升，以及防御者需要解读的证据量的暴增。”

整个事件中，Agent 访问的唯一客户内容是五组 ExploitGym/CyberGym 挑战数据集 - 从 Agent 的角度来看，它整个入侵行动的目的是为了“作弊”：直接偷取测试答案，而不是自己解决挑战。没有其他客户模型、数据集、Spaces 或包受到影响。

但这恰恰是最值得警惕的部分：当前沿模型不受额外护栏约束时，只要存在漏洞，它们就能找到。

## 同日的安全信号：GitHub 供应链防御与 AgentCore 网关

同一天，GitHub 发布了一篇关于 npm 和 GitHub Actions 供应链攻击防御的更新。过去几个月，GitHub 推出了高影响账号的保护性锁定（72 小时只读模式）、更安全的 pull_request_target 默认行为，以及对 Actions 工作流触发源的控制。这些措施直接切断了供应链攻击中最常用的初始入侵路径。

原文链接：https://github.blog/security/supply-chain-security/disrupting-supply-chain-attacks-on-npm-and-github-actions/

AWS 也在同日发布了 AgentCore Gateway 对 MCP 2026-07-28 规范的支持，这标志着 Agent 基础设施安全的标准正在快速演进。当 Agent 可以像这次事件中那样自主行动时，网关层和协议层的安全设计就不再是可选项了。

原文链接：https://aws.amazon.com/blogs/machine-learning/how-agentcore-gateway-supports-the-mcp-2026-07-28-spec/

从零日逃逸到 K8s 横向移动，从 Jinja2 注入到 Tailscale C2 - 这篇复盘不只是 Hugging Face 的一次安全事件披露，它可能是 2026 年最重要的 AI 安全文献之一。整个软件行业都需要把安全水位提上来。
